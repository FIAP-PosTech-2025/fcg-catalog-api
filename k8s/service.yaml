apiVersion: v1
kind: Service
metadata:
  name: tccatalog-api
  labels:
    app: tccatalog-api
spec:
  selector:
    app: tccatalog-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5001
  type: LoadBalancer
